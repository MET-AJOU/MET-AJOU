import { Canvas } from "@react-three/fiber";
import { MapContainer } from "./styles";

const DebateMap = () => {
  return (
    <MapContainer>
      <Canvas></Canvas>
    </MapContainer>
  );
};

export default DebateMap;
