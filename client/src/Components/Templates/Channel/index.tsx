import ChannelForm from "@Organisms/Channel";
import LoginContainer from "@Organisms/Login/LoginContainer";

const ChannelTemplate = () => {
  return (
    <LoginContainer>
      <ChannelForm />
    </LoginContainer>
  );
};

export default ChannelTemplate;
